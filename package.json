{
  "name": "mcp-gateway",
  "version": "1.0.0",
  "description": "An MCP gateway that aggregates multiple MCP servers into a unified interface",
  "type": "module",
  "main": "build/index.js",
  "scripts": {
    "build": "bunx tsc",
    "start": "bun build/index.js",
    "dev": "bunx tsx src/index.ts",
    "type-check": "bunx tsc --noEmit",
    "test": "bun run build && bunx tsx tests/e2e-test.ts",
    "test:quick": "bunx tsx tests/e2e-simple.ts",
    "test:x402": "bun run build && bunx tsx tests/e2e-x402.ts",
    "test:x402:unit": "bun run build && bunx tsx tests/unit-x402.ts",
    "test:x402:context7": "bun run build && bunx tsx tests/integration-context7-x402.ts",
    "test:x402:context7:simple": "bun run build && bunx tsx tests/integration-context7-x402-simple.ts"
  },
  "keywords": [
    "mcp",
    "gateway",
    "aggregator",
    "server",
    "proxy",
    "model-context-protocol"
  ],
  "author": "MCP Gateway",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/elizaOS/mcp-gateway.git"
  },
  "homepage": "https://github.com/elizaOS/mcp-gateway#readme",
  "bugs": {
    "url": "https://github.com/elizaOS/mcp-gateway/issues"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "x402": "^0.5.3",
    "x402-fetch": "^0.5.1",
    "yaml": "^2.6.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@upstash/context7-mcp": "^1.0.16",
    "tsx": "^4.19.1",
    "typescript": "^5.7.0"
  },
  "bin": {
    "mcp-gateway": "./build/index.js"
  }
}
